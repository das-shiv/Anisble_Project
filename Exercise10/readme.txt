This exercise shows the priority of variables in ansible, it shows in which order of priority
variables are used in ansible.



Commands:

> ansible-playbook -e USR=cliusr var_priority.yml = command line variable
