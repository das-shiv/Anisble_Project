> This exercise shows the use of roles in ansible.

> Roles are used to organize ansible tasks, handlers, variables, files in a structured manner
for ease of use and to increase re-usability in different playbooks.


Commands used:

> ansible-galaxy init my-role = To initialize a role directory.